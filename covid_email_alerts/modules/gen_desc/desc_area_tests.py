import pandas as pd


def desc_area_tests(county: str) -> str:

    output = (
        f"Over the past two weeks, there's been a total of XXX tests in {county} County and XXX % of those were "
        f"positive. Here's how the percentage of positive tests has trended in {county}:"
    )
    return output
